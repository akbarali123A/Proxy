name: Advanced Proxy Scraper
- name: Run advanced proxy scraper
  run: python main/advanced_scraper.py
on:
  schedule:
    - cron: '0 * * * *'  # Runs every hour
  workflow_dispatch:     # Allows manual triggering

jobs:
  scrape-proxies:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests aiohttp
        
    - name: List files (debugging)
      run: ls -la
        
    - name: Run advanced proxy scraper
      run: python advanced_scraper.py
      
    - name: Commit and push changes
      run: |
        git config --global user.name "GitHub Actions"
        git config --global user.email "actions@github.com"
        git add *.txt
        git diff --quiet && git diff --staged --quiet || git commit -m "Auto-update proxies [$(date +'%Y-%m-%d %H:%M')]"
        git push
